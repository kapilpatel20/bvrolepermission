Hello {{ name }}!
 {% if  admin_permissions.checkPermission('role_list') %}
        <div class="box">


            <!-- /.box-header -->
            <div class="box-body">
                <div class="row">
                        <div class="col-sm-3 pull-right">
                                <a href="{{ path('fos_user_group_list') }}">Roles</a>
                        </div>
                </div>
            </div>

            <!-- /.box-body -->
        </div>
    {% endif %}
